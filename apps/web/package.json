{
  "name": "@promptforge/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local web UI for PromptForge - OVIC/VPDSF compliant",
  "scripts": {
    "build": "tsc",
    "serve": "node server.js",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
